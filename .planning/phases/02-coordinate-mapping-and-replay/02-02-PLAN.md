---
phase: 02-coordinate-mapping-and-replay
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - painter.py
  - tests/test_coordinate_mapping.py
autonomous: false
requirements:
  - CAL-07
  - PAINT-01
  - PAINT-02
  - PAINT-03
  - PAINT-06
  - PAINT-07
  - PAINT-08

must_haves:
  truths:
    - "Countdown prints to terminal before painting starts"
    - "Each contour is painted as mouseDown -> moveTo sequence -> mouseUp"
    - "Mouse button used for strokes is configurable via painting.mouse_button"
    - "Inter-point delay is configurable via painting.inter_point_delay"
    - "Esc sets abort_flag and painting stops after current stroke completes"
    - "Progress output shows N/M strokes and percentage"
    - "mouseUp is called on all exit paths including exceptions"
    - "dry_run mode skips all painting"
  artifacts:
    - path: "painter.py"
      provides: "run_replay, _start_abort_listener, _print_progress functions; updated _DEFAULTS and DEFAULT_CONFIG_CONTENT; updated main()"
      contains: "def run_replay"
    - path: "tests/test_coordinate_mapping.py"
      provides: "Tests for replay engine behavior (mocked pyautogui)"
      min_lines: 150
  key_links:
    - from: "painter.py:main"
      to: "painter.py:run_replay"
      via: "main() calls run_replay(state) after run_calibration(state)"
      pattern: "run_replay.*state"
    - from: "painter.py:run_replay"
      to: "painter.py:compute_draw_region"
      via: "run_replay calls compute_draw_region to get draw area"
      pattern: "compute_draw_region"
    - from: "painter.py:run_replay"
      to: "painter.py:sort_contours_nearest_neighbor"
      via: "run_replay sorts contours before mapping to screen"
      pattern: "sort_contours_nearest_neighbor"
    - from: "painter.py:run_replay"
      to: "pyautogui.mouseUp"
      via: "try/finally guarantees mouseUp on all exit paths"
      pattern: "finally.*mouseUp"
---

<objective>
Add the replay engine, abort listener, progress reporting, config defaults, and main() wiring to painter.py. This completes the painting pipeline.

Purpose: This is the plan that makes the tool actually paint. It wires the coordinate mapping (from Plan 01) into a replay loop that drives pyautogui, with abort support, progress reporting, and guaranteed mouse release.

Output: run_replay(), _start_abort_listener(), _print_progress() in painter.py under `# === REPLAY ENGINE ===`; updated _DEFAULTS and DEFAULT_CONFIG_CONTENT with new painting keys; main() extended to call run_replay(); additional tests for replay behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coordinate-mapping-and-replay/02-CONTEXT.md
@.planning/phases/02-coordinate-mapping-and-replay/02-RESEARCH.md
@.planning/phases/02-coordinate-mapping-and-replay/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (will exist when this plan runs) -->

From painter.py (COORDINATE MAPPING section):
```python
def compute_draw_region(bbox: tuple, img_w: int, img_h: int) -> tuple:
    """CAL-06: Returns (offset_x, offset_y, draw_w, draw_h) as floats."""

def map_contour_to_screen(norm_contour: np.ndarray,
                           offset_x: float, offset_y: float,
                           draw_w: float, draw_h: float) -> np.ndarray:
    """CAL-06: Returns (N, 2) int32 screen pixel array."""

def sort_contours_nearest_neighbor(contours: list) -> list:
    """PAINT-04, PAINT-05: Greedy nearest-neighbor sort from (0,0)."""
```

From painter.py (existing):
```python
@dataclass
class AppState:
    config: dict = field(default_factory=dict)
    image_path: str = ""
    contours_normalized: List[np.ndarray] = field(default_factory=list)
    bbox: Optional[tuple] = None
    abort_flag: threading.Event = field(default_factory=threading.Event)
    dry_run: bool = False
    verbose: bool = False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config defaults and replay engine tests</name>
  <files>painter.py, tests/test_coordinate_mapping.py</files>
  <read_first>painter.py, tests/test_coordinate_mapping.py, .planning/phases/02-coordinate-mapping-and-replay/02-RESEARCH.md</read_first>
  <behavior>
    - test_start_delay_default: _DEFAULTS["painting"]["start_delay"] == 3
    - test_inter_point_delay_default: _DEFAULTS["painting"]["inter_point_delay"] == 0
    - test_dry_run_skips_painting: with dry_run=True, run_replay returns without calling mouseDown
    - test_mouseup_on_exception: when moveTo raises, finally block still calls mouseUp
    - test_configurable_mouse_button: button="left" in config flows through to mouseDown/mouseUp calls
    - test_abort_flag_stops_replay: abort_flag.set() before second contour, only first contour painted
    - test_progress_output_format: captured stdout contains "N/M strokes" and percentage pattern
  </behavior>
  <action>
    1. Update _DEFAULTS in painter.py to add two new keys under "painting":
       - "start_delay": 3
       - "inter_point_delay": 0

    2. Update DEFAULT_CONFIG_CONTENT in painter.py to add two new lines in the painting section:
       - `  start_delay: 3              # Seconds to count down before painting begins.`
       - `  inter_point_delay: 0        # Seconds between moveTo calls within a stroke (0 = fastest).`

    3. Add a `# === REPLAY ENGINE ===` section in painter.py between `# === COORDINATE MAPPING ===` and `# === MAIN ===`. Add three functions:

    **_print_progress(done, total, bar_width=30)** per D-07, PAINT-07:
    - Computes percentage: done/total (guard total=0)
    - Builds bar string: "#" * filled + "-" * remaining
    - Prints with \r overwrite: `\rPainting: {done}/{total} strokes ({pct:.0%}) [{bar}]`
    - Uses end="", flush=True

    **_start_abort_listener(state)** per D-10/D-11, PAINT-06:
    - Lazy import: `from pynput import keyboard as _pynput_keyboard`
    - Defines on_press callback: if key == Key.esc, state.abort_flag.set(), return False
    - Creates Listener(on_press=on_press), sets daemon=True, starts it
    - Docstring references PAINT-06

    **run_replay(state)** per PAINT-01/PAINT-02/PAINT-03/PAINT-06/PAINT-07/PAINT-08, CAL-06/CAL-07:
    - If state.dry_run: print "Dry run: skipping painting." and return
    - Read source image: cv2.imread(state.image_path), extract img_h, img_w from .shape[:2]
    - Call compute_draw_region(state.bbox, img_w, img_h)
    - Call sort_contours_nearest_neighbor(state.contours_normalized)
    - Map all contours to screen via map_contour_to_screen
    - Skip contours with len < 2
    - Countdown: read painting.start_delay (default 3), print countdown with 1s sleeps, then " GO"
    - Start abort listener: _start_abort_listener(state)
    - Read config: button from painting.mouse_button (default "right"), inter_stroke from painting.inter_stroke_delay (default 0.05), inter_point from painting.inter_point_delay (default 0)
    - Import pyautogui lazily inside function
    - Painting loop wrapped in try/finally:
      - try: for each contour, check abort_flag.is_set() -> break. mouseDown(x0, y0, button=button). moveTo for each subsequent point with optional inter_point sleep. mouseUp(button=button). Increment painted count. _print_progress(painted, total). Sleep inter_stroke (skip after last stroke per Pitfall 6).
      - finally: pyautogui.mouseUp(button=button) -- unconditional release (PAINT-08)
    - After loop: print elapsed time. If abort: "Aborted after N/M strokes." per D-13. Else: "Done. N strokes in Xs."

    4. Update main() to call run_replay(state) after run_calibration(state).

    5. Add 7 new test functions to tests/test_coordinate_mapping.py using unittest.mock to mock pyautogui. Tests mock `painter.pyautogui` (or import and mock at module level). For test_mouseup_on_exception, mock moveTo to raise RuntimeError and assert mouseUp was still called. For test_abort_flag_stops_replay, set abort_flag after first stroke via side_effect on mouseUp. For test_progress_output_format, use capsys to capture stdout.

    For all replay tests that call run_replay, create a minimal AppState with:
    - contours_normalized: list of small (3,2) float32 arrays
    - bbox: (100, 100, 500, 500)
    - image_path: use the blank_png fixture from conftest.py (or create a tmp png)
    - config: {"painting": {"start_delay": 0, "inter_stroke_delay": 0, "inter_point_delay": 0, "mouse_button": "right"}}
    - Mock cv2.imread to return a 100x100x3 numpy array so no real file I/O is needed

    6. Run: `pytest tests/test_coordinate_mapping.py -x -q`
  </action>
  <verify>
    <automated>pytest tests/test_coordinate_mapping.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_coordinate_mapping.py` returns 18 or more (11 from Plan 01 + 7 new)
    - `grep "def run_replay" painter.py` finds the function
    - `grep "def _start_abort_listener" painter.py` finds the function
    - `grep "def _print_progress" painter.py` finds the function
    - `grep "REPLAY ENGINE" painter.py` finds the section header
    - `grep "start_delay" painter.py` appears in _DEFAULTS
    - `grep "inter_point_delay" painter.py` appears in _DEFAULTS
    - `grep "run_replay" painter.py` appears in main()
    - `grep "finally" painter.py` appears in run_replay (try/finally guard)
    - `grep "PAINT-08" painter.py` appears in run_replay docstring or comment
    - `pytest tests/test_coordinate_mapping.py -x -q` exits 0 with 18+ passed
    - `pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>run_replay paints contours as mouseDown/moveTo/mouseUp strokes with configurable button/delays, nearest-neighbor ordering, countdown, progress bar, abort support, and guaranteed mouseUp via try/finally. main() calls run_replay after calibration. All 18+ tests pass including mocked replay scenarios. Full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end painting verification</name>
  <files>painter.py</files>
  <action>Human verifies the complete painting pipeline works end-to-end on a real screen with a real painting application.</action>
  <what-built>Complete Phase 2 painting pipeline: coordinate mapping with aspect ratio preservation, nearest-neighbor contour sorting, pyautogui replay engine with countdown, progress bar, Esc abort, and mouseUp safety guarantee.</what-built>
  <how-to-verify>
    1. Open a painting application (or STS2 map editor) on screen
    2. Run: `python painter.py test_image.png --verbose`
    3. Verify preview window shows contours correctly, press any key to proceed
    4. Click top-left then bottom-right of target canvas area
    5. Verify countdown prints "3... 2... 1... GO" in terminal
    6. Verify contours are painted within the bounding box with correct proportions (no stretching)
    7. Verify progress bar updates inline: "Painting: N/M strokes (XX%) [###---]"
    8. Verify painting completes with "Done. N strokes in Xs."
    9. Test abort: run again and press Esc during painting -- verify it stops cleanly after current stroke
    10. Test dry-run: `python painter.py test_image.png --dry-run` -- verify no mouse movement occurs
  </how-to-verify>
  <verify>Human confirms painting works correctly on screen</verify>
  <done>User has verified end-to-end painting, abort, and dry-run behavior</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q` -- full test suite passes
- `python painter.py --help` -- shows usage with all args
- `grep "run_replay" painter.py` -- function exists and is called in main()
- `grep "finally" painter.py` -- try/finally guard present in run_replay
</verification>

<success_criteria>
- Tool paints contours as mouse strokes within the calibrated bounding box
- Aspect ratio preserved (no stretching)
- Contours painted in nearest-neighbor order
- Esc aborts cleanly after current stroke
- Progress bar shows stroke count and percentage
- mouseUp guaranteed on all exit paths
- All config keys (start_delay, inter_point_delay, inter_stroke_delay, mouse_button) work
- Full test suite passes (Phase 1 + Phase 2 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-coordinate-mapping-and-replay/02-02-SUMMARY.md`
</output>
