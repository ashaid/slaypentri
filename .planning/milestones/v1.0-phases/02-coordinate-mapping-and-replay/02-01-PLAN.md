---
phase: 02-coordinate-mapping-and-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_coordinate_mapping.py
  - painter.py
autonomous: true
requirements:
  - CAL-06
  - PAINT-04
  - PAINT-05

must_haves:
  truths:
    - "compute_draw_region returns correct letterbox dimensions for tall bbox + wide image"
    - "compute_draw_region returns correct pillarbox dimensions for wide bbox + tall image"
    - "compute_draw_region fills entire bbox when aspect ratios match"
    - "map_contour_to_screen converts normalized [0,1] points to integer screen pixels"
    - "sort_contours_nearest_neighbor orders contours by spatial proximity from (0,0)"
    - "sort_contours_nearest_neighbor reverses a contour when far endpoint is closer"
  artifacts:
    - path: "tests/test_coordinate_mapping.py"
      provides: "Unit tests for coordinate mapping and contour sorting"
      min_lines: 80
    - path: "painter.py"
      provides: "compute_draw_region, map_contour_to_screen, sort_contours_nearest_neighbor functions"
      exports: ["compute_draw_region", "map_contour_to_screen", "sort_contours_nearest_neighbor"]
  key_links:
    - from: "painter.py:compute_draw_region"
      to: "painter.py:map_contour_to_screen"
      via: "offset_x, offset_y, draw_w, draw_h tuple"
      pattern: "compute_draw_region.*map_contour_to_screen"
    - from: "painter.py:sort_contours_nearest_neighbor"
      to: "AppState.contours_normalized"
      via: "accepts list of (N,2) float32 arrays"
      pattern: "sort_contours_nearest_neighbor"
---

<objective>
Add pure-function coordinate mapping and contour sorting to painter.py, with a full test suite.

Purpose: These are the computational core of Phase 2 -- aspect-ratio-preserving mapping from normalized [0,1] space to screen pixels, and nearest-neighbor contour ordering. Both are pure functions testable without any screen interaction.

Output: Three new functions in painter.py under a `# === COORDINATE MAPPING ===` section, plus tests/test_coordinate_mapping.py with tests covering letterbox, pillarbox, square, mapping, sorting, and reversal.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coordinate-mapping-and-replay/02-CONTEXT.md
@.planning/phases/02-coordinate-mapping-and-replay/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces from painter.py that this plan uses -->

From painter.py:
```python
@dataclass
class AppState:
    config: dict = field(default_factory=dict)
    image_path: str = ""
    config_path: str = ""
    contours_normalized: List[np.ndarray] = field(default_factory=list)
    bbox: Optional[tuple] = None          # (x1, y1, x2, y2) screen pixels
    abort_flag: threading.Event = field(default_factory=threading.Event)
    dry_run: bool = False
    verbose: bool = False
```

Contours in `contours_normalized` are `(N, 2)` float32 arrays where column 0 is x [0,1] and column 1 is y [0,1].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold and coordinate mapping functions</name>
  <files>tests/test_coordinate_mapping.py, painter.py</files>
  <read_first>painter.py, tests/conftest.py, .planning/phases/02-coordinate-mapping-and-replay/02-RESEARCH.md</read_first>
  <behavior>
    - test_letterbox: bbox=(0,0,1000,800), img 200x100 (wide). draw_w=1000, draw_h=500, offset_x=0, offset_y=150
    - test_pillarbox: bbox=(0,0,800,1000), img 100x200 (tall). draw_w=500, draw_h=1000, offset_x=150, offset_y=0
    - test_square_bbox_no_margins: bbox=(0,0,500,500), img 100x100. draw_w=500, draw_h=500, offset_x=0, offset_y=0
    - test_map_normalized_origin: normalized (0,0) maps to (offset_x, offset_y)
    - test_map_normalized_corner: normalized (1,1) maps to (offset_x+draw_w, offset_y+draw_h) approximately
    - test_map_returns_int32: result dtype is np.int32
    - test_sort_reduces_travel: 3 contours at known positions, sorted order has less total travel than original
    - test_sort_reverses_contour: contour where far endpoint is closer to cursor gets reversed
    - test_sort_no_reverse_when_start_closer: contour where start endpoint is closer stays unchanged
    - test_sort_empty_list: empty input returns empty list
    - test_sort_single_contour: single contour returned (possibly reversed toward origin)
  </behavior>
  <action>
    1. Create tests/test_coordinate_mapping.py with try/except ImportError guard and pytestmark skipif pattern (matching Phase 1 convention from test_image_pipeline.py).

    2. Write 11 test functions covering the behaviors above. Each test creates numpy arrays directly -- no fixtures needed beyond what conftest.py provides. Use approximate float comparison (pytest.approx or abs tolerance) for draw region tests since the function returns floats.

    3. In painter.py, add a `# === COORDINATE MAPPING ===` section between `# === CALIBRATION ===` and `# === MAIN ===` (before line 464). Add three functions:

    **compute_draw_region(bbox, img_w, img_h)** per D-01/D-02/D-03:
    - Takes bbox tuple (x1,y1,x2,y2), image width, image height
    - Returns (offset_x, offset_y, draw_w, draw_h) as floats
    - Letterbox/pillarbox math: if bbox_w / img_aspect <= bbox_h then width-constrained, else height-constrained
    - Centers on the shorter axis
    - Docstring references CAL-06

    **map_contour_to_screen(norm_contour, offset_x, offset_y, draw_w, draw_h)** per CAL-06:
    - Takes (N,2) float32 normalized contour + draw region params
    - Returns (N,2) int32 array of screen pixel coordinates
    - screen[:,0] = (offset_x + norm[:,0] * draw_w).astype(int32)
    - screen[:,1] = (offset_y + norm[:,1] * draw_h).astype(int32)

    **sort_contours_nearest_neighbor(contours)** per D-08/D-09, PAINT-04/PAINT-05:
    - Takes list of (N,2) float32 arrays
    - Returns new sorted list
    - Starts from (0.0, 0.0) in normalized space
    - For each remaining contour, computes squared distance to both endpoints
    - Picks closest, reverses if far endpoint was closer
    - Updates cursor to last point of chosen contour
    - Docstring references PAINT-04, PAINT-05

    4. Run tests: `pytest tests/test_coordinate_mapping.py -x -q`
  </action>
  <verify>
    <automated>pytest tests/test_coordinate_mapping.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_coordinate_mapping.py` returns 11 or more
    - `grep "def compute_draw_region" painter.py` finds the function
    - `grep "def map_contour_to_screen" painter.py` finds the function
    - `grep "def sort_contours_nearest_neighbor" painter.py` finds the function
    - `grep "COORDINATE MAPPING" painter.py` finds the section header
    - `grep "CAL-06" painter.py` appears in coordinate mapping docstrings
    - `grep "PAINT-04" painter.py` appears in sort docstring
    - `pytest tests/test_coordinate_mapping.py -x -q` exits 0 with 11+ passed
    - `pytest tests/ -x -q` exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <done>All 11+ coordinate mapping and sorting tests pass. Functions exist in painter.py with correct section header and requirement ID references. Full test suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_coordinate_mapping.py -x -q` -- all tests pass
- `pytest tests/ -x -q` -- full suite green (Phase 1 tests unaffected)
- `grep -c "def test_" tests/test_coordinate_mapping.py` -- 11+
</verification>

<success_criteria>
- compute_draw_region correctly handles letterbox, pillarbox, and square cases
- map_contour_to_screen converts [0,1] normalized coords to int32 screen pixels
- sort_contours_nearest_neighbor orders contours by proximity and reverses when beneficial
- All tests pass, no Phase 1 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-coordinate-mapping-and-replay/02-01-SUMMARY.md`
</output>
